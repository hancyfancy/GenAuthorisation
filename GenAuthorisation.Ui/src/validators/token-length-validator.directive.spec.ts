import { TokenLengthValidatorDirective } from './token-length-validator.directive';

describe('TokenLengthValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TokenLengthValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
